git:
  pagers:
    - colorArg: always
      pager: delta --dark --paging=never --side-by-side --hyperlinks
gui:
  nerdFontsVersion: "3"
  sidePanelWidth: 0.2
  showFileTree: false
  # showRandomTip: false
  # showCommandLog: false
