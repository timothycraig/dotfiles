git:
  diffRenderers:
    - colorArg: always
      command: delta --dark --paging=never --side-by-side --hyperlinks --syntax-theme="GitHub Dark Default"
gui:
  nerdFontsVersion: "3"
  sidePanelWidth: 0.2
  showFileTree: false
  # showRandomTip: false
  # showCommandLog: false
